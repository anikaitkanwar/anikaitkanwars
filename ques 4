# Program to add three numbers and convert to a string
# Defining variables according to question
a = 25
b = "25"
c = 25.0
# Calculating sum using type casting
sum = str(a + int(b) + int(c))
# Printing the output string
print(f"The final output is {sum}")
